# ============================================================
# ENVIRONMENT VARIABLES TEMPLATE
# Copy this file to .env and fill in your real values.
# The .env file is git-ignored and must NEVER be committed.
# ============================================================

# Primary admin contact for email notifications and audit alerts
# Admin accounts should be created securely with scripts/tools, not stored in .env.
ADMIN_EMAIL=your_admin@email.com
ADMIN_NAME=System Admin

# Optional RBAC admin contact emails
SUPER_ADMIN_EMAIL=super_admin@yourcompany.com
FINANCE_ADMIN_EMAIL=finance_admin@yourcompany.com
PROJECT_ADMIN_EMAIL=project_admin@yourcompany.com
SECURITY_ADMIN_EMAIL=security_admin@yourcompany.com

# JWT signing secret — use a long random string in production
JWT_SECRET=change_this_to_a_long_random_secret_in_production

# Use scripts/create-admin.js to create admin users with Argon2id-hashed passwords.
# Do NOT store admin passwords in .env, .env.example, or version control.

# Server ports
PORT_PUBLIC_API=3003
PORT_ADMIN_API=3004

# Debug OTP — set to false in production!
DEBUG_OTP=false
